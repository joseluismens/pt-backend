# PtBackend

install Node.js by msi `https://nodejs.org/en/download`.
install typescript by npm `npm i typescript -g `

## Development server

Run `npm install` for install dependencies

Run `npm start` for a dev server.

database is connected by clever-cloud , local is not necessary
